# 更新日志

各版本的发布说明。正文与 GitHub Releases 保持一致。

---

## v0.1.3 — 2026-09-14

**修复**

- 高亮默认跟随输入所属的进制行：输入 `0xffff` 高亮十六进制行、输入 `255` 高亮十进制行（原来固定落在十进制）
- 源行标记从灰底改为 2px 左侧竖条，不再与键盘焦点环叠加成两个底色
- 构建目标锁定 Chromium 91（uTools 内置 Chromium 91 + Node.js 14，官方 FAQ），避免较新语法在插件内出问题

**其他**

- 市场截图从 `public/` 移到 `docs/screenshots/`，不再被 Vite 拷进插件包（插件包约 144 KB）
- 新增本文件与应用市场发布文案 `docs/应用市场文案.md`

## v0.1.2 — 2026-09-14

**修复**

- logo 符合 uTools 规范：改为 256×256 真 PNG（原为 2048×2048 的 JPEG 套 `.png` 后缀），体积 1.26 MB → 49 KB

## v0.1.1 — 2026-09-14

**变更**

- 更换插件 logo

## v0.1.0 — 2026-09-14 · 首个版本

**新增**

- 四进制联动界面：HEX / DEC / BIN / OCT 实时互转，点击任意行即可复制（不带分组空格）
- 前缀自动识别：`0x` / `0b` / `0o`，并可手动指定按 N 进制解析
- 按位宽解释：8 / 16 / 32 / 64 位下的无符号值与有符号补码
- 字节序视图：大端 / 小端字节序列
- 容错解析：全角字符、空格 / 下划线 / 逗号分隔、负数、任意位宽用 `BigInt` 不丢精度
- uTools 指令：1 条功能指令 + `0x` / `0b` / `0o` 三条正则匹配指令
- 键盘操作：`↑` / `↓` 选行、`Enter` 复制、`⌘/Ctrl + Enter` 粘贴回原窗口
- shadcn/ui 默认主题配色（深浅色跟随系统），窗口高度自适应
- 转换核心独立为纯函数 `src/utils/radix.js`，接入 Vitest（38 条单元测试）
